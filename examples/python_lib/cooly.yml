archive:
  name: python_lib
  repo: local@/local/repository/path/to/python_lib
  tree_ish:
  output:

build:
  host: builder@building-server
  toolbin: /remote/binary/path/to/cooly/tool
  output: /local/path/to/store/distributions
  requirements:
  pre_script:
  post_script:

install:
  hosts:
    - installer@production-server
  path: /remote/path/to/install/python_lib
  pre_command:
  post_command:
  max_versions: 5
